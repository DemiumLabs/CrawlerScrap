// var config = require('./config.js');
// const fs = require('fs');
// var sanitizeHtml = require('sanitize-html');


// fs.readdir('files/', function(err, items) {

//     items.forEach((file)=>{
//         fs.readFile('files/'+file,(err,data)=>{
//              fs.writeFile('files/'+file, sanitizeHtml(data),(r)=>console.log(r));
//         })

//     })

// });